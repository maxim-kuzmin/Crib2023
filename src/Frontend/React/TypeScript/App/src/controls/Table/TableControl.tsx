import React from 'react';
import { Table } from 'antd';
import { type TableControlParams } from '../../all';

export const TableControl: React.FC<TableControlParams> = ({ controlColumns }: TableControlParams) => {
  return <Table/>
}
